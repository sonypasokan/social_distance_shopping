import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
//this class has not been used yet. but can be used to send token along with every request.
export class RequestInterceptorService {

  constructor() { }
}
